<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/diabetes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/diabetes"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YourProfile"></activity>
        <activity android:name=".Statistic" />
        <activity android:name=".Add_Measur" />
        <activity android:name=".AddedMeals" />
        <activity android:name=".Add_Meals" />
        <activity android:name=".Meals" />
        <activity android:name=".Login_in" />
        <activity android:name=".Sign_Up" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>